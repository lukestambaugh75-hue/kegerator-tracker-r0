version = 1
id = "kegerator-tracker-email"
kind = "cron"
name = "Kegerator tracker email"
prompt = """Run the Kegerator Tracker refresh, public dashboard verification, and approved email delivery.

Work in `/Users/lukestambaugh/Documents/Files for GitHub/Kegerator Tracker r0`.

This automation is explicitly approved to email exactly these two recipients:
- `lukestambaugh75@gmail.com`
- `devin.mullen89@gmail.com`

Do not add CC or BCC. This approval is scoped only to this kegerator tracker.
This is a direct-link-only Kegerator page. Do not add links, navigation, or runtime assets from any other dashboard or repository.

Required run order:
1. Run `git pull --ff-only`.
2. Create one unique run ID and run `/usr/bin/python3 scripts/run_evidence.py start --run-id "$RUN_ID" --lane-id scheduled-email --owner-pid "$$"`. The ignored, nonsymlink `out/run-state.json` is the terminal summary; never add it to git. If an unfinished prior run occupies the lane, do not overwrite it. `scripts/run_evidence.py recover-stale --expected-run-id <exact-old-id>` is allowed only when the adapter proves the recorded owner is dead and the run is at least 12 hours old.
3. Run `/usr/bin/python3 scripts/run_evidence.py refresh`. The adapter itself executes the fixed refresh command exactly once, binds the actual pre-run target count and manifest, and writes a run-specific ignored outcome. Never run `scripts/refresh.py` separately in this lane.
4. Run `/usr/bin/python3 scripts/run_evidence.py verify`. The adapter itself executes and records the fixed `make verify-current` command, including `scripts/audience_guard.py`, path, exit code, timestamps, and output digests.
5. Do not improvise repairs. Only when recorded verification fails, try the allowlisted `history-prune` precheck with `scripts/run_evidence.py propose-repair --repair-id history-prune`. If it proves applicable, run the single fixed attempt with `scripts/run_evidence.py repair --repair-id history-prune`, then rerun `scripts/run_evidence.py verify`. Any other failure is a blocker. Never make a second repair or verification retry in the same run.
6. If tracked files changed and the branch is `main`, stage only touched repo files, commit with `Refresh Kegerator dashboard data`, and push to origin. Do not force-push. Then bind the clean pushed result and live origin identity with `/usr/bin/python3 scripts/run_evidence.py result`.
7. If refresh evidence is blocked, partial, or failed, keep receipt `unverified`, run `/usr/bin/python3 scripts/run_evidence.py finish --outcome blocked`, and do not send. Otherwise continue.
8. Run `/usr/bin/python3 scripts/run_evidence.py deployment`. Retry only for ordinary Pages propagation. The recorder itself must fetch every canonical public URL and compare exact response bytes to Git blobs from the bound result SHA while rechecking the approved live origin.
9. Bind the one canonical payload with `/usr/bin/python3 scripts/run_evidence.py payload --payload out/latest-email.json`. This rejects extra fields, alternate serialization, stale generation time, changed content, source drift, or anything other than the deterministic fresh payload.
10. Read `out/latest-email.json` and make at most one Gmail send attempt using the signed-in Chrome/Gmail browser route. Address it exactly to `lukestambaugh75@gmail.com` and `devin.mullen89@gmail.com`, with no CC/BCC, using the generated subject and body. Include the public dashboard link `https://lukestambaugh75-hue.github.io/kegerator-tracker-r0/`.
11. Before that one attempt, visibly verify the two recipient chips, no CC/BCC, subject, body, dashboard link, and the recorded passing audience guard. If the Gmail connector reports missing OAuth scopes, use the already approved signed-in Chrome/Gmail browser route; do not repair the connector during the run and do not retry the send.
12. This repository has no trusted external receipt adapter. Detached JSON, screenshots, or AI-authored observations cannot verify delivery. Receipt must remain `unverified`; after the one attempt run `/usr/bin/python3 scripts/run_evidence.py finish --outcome delivery_unverified`. Never claim `delivered`.
13. If an external command fails after start, run `scripts/run_evidence.py finalize-failure --stage <bounded-stage> --reason-code <bounded-code>` before stopping. Adapter-owned command failures finalize automatically except a first verification failure, which stays open only for the one allowlisted repair decision.
14. After the terminal outcome is recorded, stop and wait for the next scheduled run.

Do not purchase anything or contact retailers. Confirm final cart total, tax, pickup/delivery timing, seller identity, keg fit, and garage ventilation before buying."""
status = "ACTIVE"
rrule = "RRULE:FREQ=DAILY;BYHOUR=6;BYMINUTE=0;BYSECOND=0"
model = "gpt-5.5"
reasoning_effort = "high"
execution_environment = "local"
cwds = ["/Users/lukestambaugh/Documents/Files for GitHub/Kegerator Tracker r0"]
